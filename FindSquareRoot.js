const number = 190;

const result = Math.sqrt(number);

console.log(result);

const funNum = (num) => {
  const result = Math.sqrt(num);
  console.log(`${num} square root is ${result}`);
};

funNum(9);
